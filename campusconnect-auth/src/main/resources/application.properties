spring.application.name=campusconnect-auth

# Server config
server.port=8080

# DB connection
spring.datasource.url=jdbc:mysql://localhost:3306/campusconnect
spring.datasource.username=root
spring.datasource.password=harish_07

# JPA config
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# JWT (we'll define this in code)
jwt.secret=ThisIsCampusConnectSecretKeyForJWTGeneration123456
# JWT expiration in milliseconds (1 day)
jwt.expiration=86400000


# application.properties
upload.directory=uploads

